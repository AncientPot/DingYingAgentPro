# DingYingAgentPro 开发过程

## 第一阶段：CLI → FastAPI 模块化重构

### 初始需求
用户提供 `基础代码.py`（一个基于 LangGraph + DeepSeek 的 CLI 智能助手程序），要求在保证模块化设计的前提下将其改为 FastAPI 后端，留好接口方便后续前端创作。

### 方案设计
用户进一步要求：
1. 留出设置 API、模型、模型温度、系统提示词的接口（供设置页面使用）
2. 重点维护工具文件夹，每个工具单独一个 `.py` 文件，支持自定义增删，写好工具管理接口

最终确定分层架构：
```
app/
├── main.py                   # FastAPI 入口
├── core/config.py            # 运行时配置管理（JSON持久化+热重载）
├── agent/graph.py            # LangGraph 图构建与缓存
├── tools/                    # 插件式工具目录（增删.py即生效）
│   ├── __init__.py           # 自动发现加载器
│   ├── calculator.py
│   ├── tavily_search.py
│   └── netease_cloud_music.py
├── models/schemas.py         # Pydantic 模型
├── services/                 # 业务逻辑层
│   ├── session_service.py
│   ├── config_service.py
│   └── tool_service.py
└── api/                      # 路由层
    ├── chat.py               # 流式SSE+非流式
    ├── sessions.py
    ├── config.py
    └── tools.py
```

### 实现操作
1. 创建 `app/` 全套包结构（17 个源文件）
2. `core/config.py`：配置三层优先级（config.json > 环境变量 > 默认值），支持脱敏和热重载回调
3. `agent/graph.py`：从原代码抽取 State + Agent 节点 + 图编译，支持单例缓存
4. `tools/__init__.py`：`pkgutil.iter_modules` 自动扫描，`importlib` 动态加载，导入失败自动跳过
5. 将原代码中 calculator、netease_cloud_music、tavily_search 拆分为独立工具文件
6. `api/chat.py`：非流式 `POST /api/chat` + SSE 流式 `POST /api/chat/stream`
7. `api/sessions.py`：会话 CRUD
8. `api/config.py`：设置读写
9. `api/tools.py`：工具列表 + 启用/禁用
10. `services/` 层封装业务逻辑

### 启动验证
- 启动 uvicorn → 端口被旧进程占用 → `taskkill /F /PID` 清理
- `curl` 测试 `/api/health`、`/api/config`、`/api/tools`、`/api/chat`、`/api/chat/stream` 全部通过
- 提交 `fcdef4a`：23 个文件，1439 行新增

---

## 第二阶段：Vue3 前端开发

### 设计阶段
用户要求使用 Vite + Vue 框架，设计参考 frontend-design skill。

确定美学方向：**"精密仪器 Precision Terminal"** —— 暗色终端美学，电光青 `#00e5ff` 点缀，玻璃拟态面板。

技术栈：Vue 3 Composition API + Pinia + Vue Router + TailwindCSS v3 + Vite 6。

创建 `frontend/` 目录，19 个源文件：
- `src/views/ChatView.vue`：SSE 流式对话主页
- `src/views/SettingsView.vue`：6 项配置表单
- `src/views/ToolsView.vue`：工具管理卡片网格
- `src/components/`：TopNav、SessionSidebar、ChatMessage、ToolCallCard、ChatInput
- `src/stores/`：session、config、tools 三个 Pinia store
- `src/api/index.js`：全部 API 封装 + SSE 异步生成器
- `src/style.css`：全局样式（玻璃面板、toggle 开关、消息动画、扫描线背景）
- `vite.config.js`：`/api` 代理到 `localhost:8000`

### 首次 Bug 修复

用户反馈四个问题：

**1. 界面偏暗**
- 根因：背景 `#06060a` 过暗，文字对比度不足
- 修复：Tailwind 色板整体提亮，玻璃面板透明度 `0.03→0.05`，文字 `white/70→white/85`

**2. 切换页面后对话历史丢失**
- 根因：路由切换时组件销毁，`messages: ref([])` 重置
- 修复：`router-view` 包裹 `<keep-alive>` 缓存组件实例

**3. 流式传输不生效**
- 根因：`const aiMsg = {...}; messages.value.push(aiMsg)` 后 `aiMsg` 指向原始对象而非 Vue 响应式代理
- 修复：`messages.value.push(...)` 后再从数组取引用 `const aiMsg = messages.value[messages.value.length - 1]`

**4. 网易云音乐工具调用报错**
- 根因：`invoke()` 失败时 pywinauto 内部对象转字符串暴露给用户，窗口未获焦点时控件不可交互
- 修复：新增 `_safe_invoke()` 含重试机制 + `set_focus()` + 异常分类处理

### 提交
`855f174`：33 个文件，4301 行新增

---

## 第三阶段：系统提示词修复 + 工具测试接口

### 系统提示词不生效（两次排查）

**第一次修复（epoch 缓存）**
- 用户反馈系统提示词不生效
- 将 `agent/graph.py` 的哈希缓存替换为版本计数器（epoch），消除 `hash()` 返回 0 导致不重建的风险

**第二次修复（真正的根因）**
- 用户反馈修复后仍不生效，要求"本次存储系统设置"
- 全链路追查：前端 SettingsView → API → 配置持久化 → 图重建 → LLM 调用
- 发现根因链：
  1. `get_settings()` 将 `enabled_tools` 列表强转为字符串 `"['...']"`
  2. 前端 form 初始化后包含此字符串
  3. 保存时发送全部字段（含非法 `enabled_tools` 字符串）
  4. Pydantic 验证 `list[str]` 失败 → HTTP 422 → 整个请求被拒
  5. `system_prompt` 虽正确但从未到达 `update_config()`
  6. 前端无错误提示，用户感知为"不生效"

- 修复（4 处联动）：
  - `config_service.py`：`get_settings()` 不再 `str()` 一切，保留原生类型
  - `schemas.py`：`ConfigItem.value` 类型 `str→Any`
  - `SettingsView.vue`：`handleSave()` 只发送可见字段，排除 `enabled_tools`
  - `stores/config.js`：`saveConfig()` 加 `error` 状态 + UI 红字提示

### 工具测试接口

用户要求"为每个工具提供一个测试接口，让 AI 知道哪个工具可用"。

新增 `POST /api/tools/{name}/test`：
- calculator：执行 3+4、10-3、3×3 验证
- tavily_search：检查 API Key + 发送搜索请求
- netease_cloud_music：检查 exe 存在 + 窗口定位
- 前端工具管理页每个卡片新增"测试"按钮

### 重写 README_API.md
包含全部 9 个接口详解、SSE 事件表、工具插件开发完整指南、前端集成映射。

---

## 第四阶段：Code Review 与修复

### 审查方法
执行 3 角度扫描（逐行 diff、移除行为审计、跨文件追踪）→ 18 条候选项 → 逐一验证 → 7 条确认/合理。

### 发现与修复

| 严重度 | 问题 | 文件 |
|:---:|------|------|
| 严重 | 脱敏 API Key 覆盖 | `SettingsView.vue` |
| 高 | `[] or available` falsy | `tools/__init__.py` |
| 中 | SSE tool 事件 lastTC 错配 | `chat.py` + `ChatView.vue` |
| 低 | get_graph TOCTOU 竞态 | `graph.py` |
| 低 | _sync 不相交集合 | `session_service.py` |
| 低 | JSON.parse 无 try-catch | `api/index.js` |
| 低 | _safe_invoke 字符串耦合 | `netease_cloud_music.py` |

**修复详情**：
1. `api_key` 含 `*` 时跳过发送，防止脱敏值覆盖真实密钥
2. `[] or available` → `set(enabled_tools) if enabled_tools is not None`
3. 后端输出 `tool_call_id`，前端按 ID 匹配替代 `lastTC`
4. `invalidate_graph()` 同时置 `_graph=None` 缩窄窗口
5. `>/<` 严格子集 → 独立 `set.difference` 检查
6. `JSON.parse` 包裹 `try-catch`，畸形行跳过不终止流
7. 新增 `UIElementError` 异常类替代字符串匹配

### 提交
`0ca152c`：9 个文件，143 行新增

---

## 第五阶段：项目文档

### CLAUDE.md
创建项目架构文档，涵盖：常用命令、分层架构、配置系统、工具插件规范、会话持久化、流式传输关键约束、图缓存与锁机制、前端架构。

### 过程.md（本文档）
记录从项目开始到现在的完整开发过程。

---

## 遇到的技术问题汇总

| 问题 | 根因 | 解决 |
|------|------|------|
| 端口被占用 | 旧 uvicorn 进程未清理 | `taskkill /F /PID` |
| 中文引号语法错误 | 全角引号被 Python 解析为字符串分隔符 | 改用单引号 |
| Google Fonts 加载慢 | 国内被墙，preconnect 阻塞 | 改用系统原生字体 |
| Vue 流式不更新 | push 前引用非响应式对象 | push 后从数组取代理引用 |
| 系统提示词不生效 | 前端发送非法类型致 422 | 保原生类型 + 排除非可见字段 |
| API Key 被覆盖 | 脱敏值回写 | 含 `*` 则跳过发送 |

## Git 提交记录

```
fcdef4a  将CLI程序重构为模块化FastAPI后端
855f174  新增Vue3前端 + 修复系统提示词 + 工具测试接口
0ca152c  修复7个代码审查问题
```
